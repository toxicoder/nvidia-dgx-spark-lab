# Python tooling for the documentation pipeline.
#
# The site itself is the Node app in docs-site/ (see docs-site/package.json); these are the
# Python tools the docs generators and Bazel gates still run:
#   - pytest + pytest-cov: //docs:test_python_coverage enforces 100% coverage on the
#     generator modules.
#   - Pillow: pixel comparison used when triaging a visual-regression failure in
#     docs-site/tests/visual.
pytest>=8.0.0
pytest-cov>=6.0.0
Pillow>=10.0.0
