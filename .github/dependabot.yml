version: 2
updates:
  # GitHub Actions used in workflows (e.g. actions/checkout, etc.)
  # Open PRs against development (GitHub default branch is main).
  - package-ecosystem: "github-actions"
    directory: "/"
    target-branch: development
    schedule:
      interval: "weekly"
    groups:
      github-actions:
        patterns:
          - "*"

  # npm for dashboard (package.json + package-lock.json)
  - package-ecosystem: "npm"
    directory: "/dashboard"
    target-branch: development
    schedule:
      interval: "weekly"
    groups:
      npm-dependencies:
        patterns:
          - "*"

  # pip for docs (requirements.txt)
  - package-ecosystem: "pip"
    directory: "/docs"
    target-branch: development
    schedule:
      interval: "weekly"
    groups:
      pip-dependencies:
        patterns:
          - "*"
