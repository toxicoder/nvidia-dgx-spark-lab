# Example: create the dashboard secrets vault master key (do not commit real values).
#
# kubectl create secret generic lab-dashboard-secrets -n dev \
#   --from-literal=master-key="$(openssl rand -base64 32)"
#
# Or use: ./scripts/manage.sh secrets ensure-key
#
# Loss of this key makes encrypted vault data unrecoverable. Back up separately from auth secret.