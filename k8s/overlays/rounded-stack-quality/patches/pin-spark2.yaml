# Purpose: Pin Qwen3.6 27B to spark2 for Mode A --quality swap
# Source of truth: k8s/overlays/rounded-stack-quality/
# Regenerate: n/a
# Safety: overlay only; base 27B Job stays unpinned for 1-node exclusive/dual

apiVersion: batch/v1
kind: Job
metadata:
  name: ignored
spec:
  template:
    spec:
      nodeSelector:
        kubernetes.io/hostname: spark2
