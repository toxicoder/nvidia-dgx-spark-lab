"""Package purpose: Mode A rounded-stack quality overlay (27B on spark2)."""

package(default_visibility = ["//visibility:private"])

filegroup(
    name = "rounded_stack_quality",
    srcs = glob(["**/*"]),
    visibility = [
        "//k8s:__subpackages__",
        "//scripts:__pkg__",
        "//tests:__pkg__",
    ],
)
