# Purpose: Kustomize bundle for nemotron parse workload
# Source of truth: k8s/workloads/nemotron-parse/
# Regenerate: n/a
# Safety: manual start via manage.sh; heavy GPU resources when Jobs run

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - nemotron-parse-deployment.yaml
  - service.yaml
