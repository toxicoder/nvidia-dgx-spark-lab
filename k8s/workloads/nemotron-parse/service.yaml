# Purpose: Kubernetes Service for nemotron parse inference endpoint
# Source of truth: k8s/workloads/nemotron-parse/
# Regenerate: n/a
# Safety: exposes inference API; protect with SSO ingress

apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8000"
    prometheus.io/path: "/metrics"
  name: nemotron-parse
  namespace: ai-inference
  labels:
    app: nemotron-parse
spec:
  selector:
    app: nemotron-parse
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  type: ClusterIP
