# Purpose: Kustomize bundle for glm 5.2 workload
# Source of truth: k8s/workloads/glm-5.2/
# Regenerate: n/a
# Safety: manual start via manage.sh; heavy GPU resources when Jobs run

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - glm-5.2-rpc-job.yaml
  - glm-5.2-job.yaml
  - service.yaml
