# Purpose: Kustomize bundle for kimi test workload
# Source of truth: k8s/workloads/kimi-test/
# Regenerate: n/a
# Safety: manual start via manage.sh; heavy GPU resources when Jobs run

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - kimi-test-job.yaml
  - service.yaml
