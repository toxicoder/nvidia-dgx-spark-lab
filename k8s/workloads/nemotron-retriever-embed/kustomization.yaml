# Purpose: Kustomize bundle for nemotron retriever embed workload
# Source of truth: k8s/workloads/nemotron-retriever-embed/
# Regenerate: n/a
# Safety: manual start via manage.sh; heavy GPU resources when Jobs run

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - nemotron-retriever-embed-deployment.yaml
  - service.yaml
