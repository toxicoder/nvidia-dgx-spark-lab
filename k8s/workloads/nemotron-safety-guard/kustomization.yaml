# Purpose: Kustomize bundle for nemotron safety guard workload
# Source of truth: k8s/workloads/nemotron-safety-guard/
# Regenerate: n/a
# Safety: manual start via manage.sh; heavy GPU resources when Jobs run

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - nemotron-safety-guard-deployment.yaml
  - service.yaml
