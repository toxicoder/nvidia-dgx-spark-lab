# Purpose: Kustomize bundle for nemotron 3 nano omni 30b workload
# Source of truth: k8s/workloads/nemotron-3-nano-omni-30b/
# Regenerate: n/a
# Safety: manual start via manage.sh; heavy GPU resources when Jobs run

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - nemotron-3-nano-omni-30b-job.yaml
  - service.yaml
