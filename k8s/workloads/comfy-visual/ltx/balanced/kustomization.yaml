# Purpose: LTX-2.3 balanced overlay on comfy-base
# Source of truth: k8s/workloads/comfy-visual/ltx/balanced/
# Regenerate: n/a
# Safety: manual start only; exclusive GPU visual workload

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: ltx-balanced-workflow
    files:
      - lab-ltx-balanced.json=workflows/lab-ltx-balanced.json
    options:
      labels:
        app: ltx-balanced
        workload: visual
        family: ltx
        mode: balanced
      disableNameSuffixHash: true

resources:
  - ../../../comfy-base

patches:
  - target:
      kind: Deployment
      name: comfy-base
    path: patches/deployment.json
  - target:
      kind: Service
      name: comfy-base
    path: patches/service.json
