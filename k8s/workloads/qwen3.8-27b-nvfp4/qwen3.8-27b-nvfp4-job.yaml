# Purpose: Qwen3.8 27B NVFP4 dense inference Job (exclusive 1-node Spark)
# Source of truth: k8s/workloads/qwen3.8-27b-nvfp4/
# Regenerate: n/a
# Safety: manual start only; gpu-mem-util 0.72 exclusive; fail-stop Job

apiVersion: batch/v1
kind: Job
metadata:
  name: qwen3.8-27b-nvfp4
  namespace: ai-inference
  labels:
    app: qwen3.8-27b-nvfp4
    workload: inference
    mode: production
    family: qwen3.8
spec:
  backoffLimit: 1
  ttlSecondsAfterFinished: 7200
  template:
    metadata:
      labels:
        app: qwen3.8-27b-nvfp4
        workload: inference
        mode: production
        family: qwen3.8
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      restartPolicy: OnFailure
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: nvidia-dgx-spark/highspeed
                    operator: Exists
      containers:
        - name: inference
          # Unsloth dynamic NVFP4 + native MTP; Blackwell GB10 needs CUTE_DSL_ARCH=sm_121a.
          image: vllm/vllm-openai:cu130-nightly
          imagePullPolicy: IfNotPresent

          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]

          resources:
            requests:
              cpu: "8"
              memory: "48Gi"
              nvidia.com/gpu: "1"
            limits:
              cpu: "16"
              memory: "56Gi"
              nvidia.com/gpu: "1"
          env:
            - name: NCCL_SOCKET_IFNAME
              value: "enp1s0f0np0,enp1s0f1np1"
            - name: NCCL_IB_HCA
              value: "mlx5_0,mlx5_1"
            - name: NCCL_DEBUG
              value: "INFO"
            - name: NCCL_P2P_DISABLE
              value: "0"
            - name: NCCL_SHM_DISABLE
              value: "0"
            - name: NCCL_IB_DISABLE
              value: "0"
            - name: CUTE_DSL_ARCH
              value: "sm_121a"
            - name: MODEL_NAME
              value: "unsloth/Qwen3.8-27B-NVFP4"
            - name: HF_HOME
              value: "/models"
            - name: VLLM_PORT
              value: "8000"
            - name: VLLM_ALLOW_LONG_MAX_MODEL_LEN
              value: "1"
          ports:
            - containerPort: 8000
              name: http
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /v1/models
              port: 8000
            initialDelaySeconds: 180
            periodSeconds: 15
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /v1/models
              port: 8000
            initialDelaySeconds: 360
            periodSeconds: 30
            timeoutSeconds: 10
          command: ["/bin/sh", "-c"]
          args:
            - >
              exec vllm serve "${MODEL_NAME}"
              --host 0.0.0.0
              --port 8000
              --tensor-parallel-size 1
              --trust-remote-code
              --served-model-name qwen3.8-27b
              --quantization compressed-tensors
              --attention-backend triton_attn
              --reasoning-parser qwen3
              --enable-auto-tool-choice
              --tool-call-parser qwen3_coder
              --enable-prefix-caching
              --enable-chunked-prefill
              --kv-cache-dtype fp8
              --gpu-memory-utilization 0.72
              --max-model-len 131072
              --max-num-seqs 4
              --speculative-config '{"method":"mtp","num_speculative_tokens":3}'
              --limit-mm-per-prompt image=4
          volumeMounts:
            - name: model-storage
              mountPath: /models
            - name: shm
              mountPath: /dev/shm
      volumes:
        - name: model-storage
          hostPath:
            path: /mnt/models
            type: DirectoryOrCreate
        - name: shm
          emptyDir:
            medium: Memory
            sizeLimit: 16Gi
