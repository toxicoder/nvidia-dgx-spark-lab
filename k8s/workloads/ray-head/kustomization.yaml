# Purpose: Kustomize bundle for ray head workload
# Source of truth: k8s/workloads/ray-head/
# Regenerate: n/a
# Safety: manual start via manage.sh; heavy GPU resources when Jobs run

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ray-head-job.yaml
  - ray-head-service.yaml
