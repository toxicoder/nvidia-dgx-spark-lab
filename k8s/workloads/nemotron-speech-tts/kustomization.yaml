# Purpose: Kustomize bundle for nemotron speech tts workload
# Source of truth: k8s/workloads/nemotron-speech-tts/
# Regenerate: n/a
# Safety: manual start via manage.sh; heavy GPU resources when Jobs run

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - nemotron-speech-tts-deployment.yaml
  - service.yaml
