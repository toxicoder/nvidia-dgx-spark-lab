# Purpose: Example Secret for LiteLLM master key (do not commit real values)
# Source of truth: k8s/workloads/litellm/secrets.example.yaml
# Regenerate: n/a
# Safety: example only; never commit live keys

apiVersion: v1
kind: Secret
metadata:
  name: litellm-secrets
  namespace: ai-inference
  labels:
    app: litellm
type: Opaque
stringData:
  master-key: "sk-change-me"
