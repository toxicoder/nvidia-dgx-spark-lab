# Purpose: Kubernetes Service for nemotron retriever rerank inference endpoint
# Source of truth: k8s/workloads/nemotron-retriever-rerank/
# Regenerate: n/a
# Safety: exposes inference API; protect with SSO ingress

apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8000"
    prometheus.io/path: "/metrics"
  name: nemotron-retriever-rerank
  namespace: ai-inference
  labels:
    app: nemotron-retriever-rerank
spec:
  selector:
    app: nemotron-retriever-rerank
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  type: ClusterIP
