# Generated by render-domains.sh — do not edit.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: oauth2-proxy
  namespace: auth
  labels:
    app: oauth2-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oauth2-proxy
  template:
    metadata:
      labels:
        app: oauth2-proxy
    spec:
      priorityClassName: lab-management
      containers:
        - name: oauth2-proxy
          image: quay.io/oauth2-proxy/oauth2-proxy:v7.8.2
          args:
            - --provider=oidc
            - --oidc-issuer-url=https://auth.lab.local
            - --client-id=oauth2-proxy
            - --client-secret-file=/secrets/oauth2-proxy-client-secret
            - --cookie-secret-file=/secrets/cookie-secret
            - --email-domain=*
            - --http-address=0.0.0.0:4180
            - --redirect-url=https://oauth.lab.local/oauth2/callback
            - --reverse-proxy=true
            - --upstream=static://202
            - --cookie-secure=true
            - --cookie-samesite=lax
            - --cookie-domain=.lab.local
            - --whitelist-domain=.lab.local
            - --skip-provider-button=true
          ports:
            - containerPort: 4180
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          volumeMounts:
            - name: secrets
              mountPath: /secrets
              readOnly: true
      volumes:
        - name: secrets
          projected:
            sources:
              - secret:
                  name: oauth2-proxy-secrets
                  items:
                    - key: cookie-secret
                      path: cookie-secret
              - secret:
                  name: sso-oidc-clients
                  items:
                    - key: oauth2-proxy-client-secret
                      path: oauth2-proxy-client-secret
