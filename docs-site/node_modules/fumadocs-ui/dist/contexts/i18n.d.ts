import { ReactNode } from "react";
import { I18nConfig } from "fumadocs-core/i18n";
//#region src/contexts/i18n.d.ts
interface LocaleItem {
  name: string;
  locale: string;
}
interface LocaleContextType {
  locale?: string;
  onChange?: (v: string) => void;
  locales?: LocaleItem[];
}
export declare function useI18n(): LocaleContextType;
export interface I18nProviderProps extends Partial<Pick<I18nConfig, 'defaultLanguage' | 'hideLocale'>> {
  /**
   * Current locale
   */
  locale?: string;
  /**
   * Handle changes to the locale, redirect user when not specified.
   */
  onLocaleChange?: (v: string) => void;
  /**
   * Translations of current locale
   */
  translations?: Partial<Record<string, string>>;
  /**
   * Available languages
   */
  locales?: LocaleItem[];
  children?: ReactNode;
}
export declare function I18nProvider({ locales, locale, defaultLanguage, hideLocale, onLocaleChange, children, translations }: I18nProviderProps): import("react").JSX.Element;
//#endregion