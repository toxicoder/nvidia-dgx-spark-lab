import { frontmatter } from "fumadocs-core/content/md/frontmatter";
import { z } from "zod";
import fs from "node:fs/promises";
import path from "node:path";
import { createHash } from "node:crypto";
//#region src/loaders/mdx/index.ts
const querySchema = z.looseObject({
	only: z.literal(["frontmatter", "all"]).default("all"),
	collection: z.string().optional(),
	workspace: z.string().optional(),
	macro_id: z.string().optional()
});
const cacheEntry = z.object({
	code: z.string(),
	map: z.any().optional(),
	hash: z.string().optional()
});
function createMdxLoader({ getCore }) {
	return { async load({ getSource, development: isDevelopment, query, compiler, filePath }) {
		let core = await getCore();
		const macro = core.macro;
		const value = await getSource();
		const matter = frontmatter(value);
		const { collection: collectionName, workspace, only, macro_id: macroId } = querySchema.parse(query);
		if (workspace) core = core.getWorkspaces().get(workspace) ?? core;
		let after;
		const { experimentalBuildCache = false } = core.getConfig().global;
		if (!isDevelopment && experimentalBuildCache) {
			const cacheDir = experimentalBuildCache;
			const cacheKey = `${(macroId ?? collectionName ?? "global").replace(/[^a-zA-Z0-9_-]/g, "_")}_${generateCacheHash(filePath)}`;
			const cached = await fs.readFile(path.join(cacheDir, cacheKey), "utf-8").then((content) => cacheEntry.parse(JSON.parse(content))).catch(() => null);
			if (cached && cached.hash === generateCacheHash(value)) return cached;
			after = async () => {
				await fs.mkdir(cacheDir, { recursive: true });
				await fs.writeFile(path.join(cacheDir, cacheKey), JSON.stringify({
					...out,
					hash: generateCacheHash(value)
				}));
			};
		}
		let docCollection;
		if (macro && macroId !== void 0) {
			const resolved = await macro.resolve(macroId);
			for (const input of resolved.inputs) compiler.addDependency(input);
			const item = resolved.collection;
			if (item.type === "docs") docCollection = item.docs;
			else if (item.type === "doc") docCollection = item;
		} else {
			const collection = collectionName ? core.getCollection(collectionName) : void 0;
			switch (collection?.type) {
				case "doc":
					docCollection = collection;
					break;
				case "docs": docCollection = collection.docs;
			}
		}
		if (docCollection) matter.data = await core.transformFrontmatter({
			collection: docCollection,
			filePath,
			source: value
		}, matter.data);
		if (only === "frontmatter") return {
			code: `export const frontmatter = ${JSON.stringify(matter.data)}`,
			map: null
		};
		const { buildMDX } = await import("./build-BPukCt-X.js").then((n) => n.n);
		const compiled = await buildMDX(core, docCollection, {
			isDevelopment,
			source: "\n".repeat(countLines(matter.matter)) + matter.content,
			filePath,
			frontmatter: matter.data,
			_compiler: compiler,
			environment: "bundler"
		});
		const out = {
			code: compiled.code,
			moduleType: "js",
			map: compiled.map
		};
		await after?.();
		return out;
	} };
}
function generateCacheHash(input) {
	return createHash("md5").update(input).digest("hex");
}
function countLines(s) {
	let num = 0;
	for (const c of s) if (c === "\n") num++;
	return num;
}
//#endregion
export { createMdxLoader as t };
