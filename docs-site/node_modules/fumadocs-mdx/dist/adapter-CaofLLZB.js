import { i as buildConfig, n as ValidationError } from "./core-BHawVsTm.js";
import { t as loadConfig } from "./load-from-file-DSDlZhTM.js";
import { readFileSync } from "node:fs";
import fs from "node:fs/promises";
import { fileURLToPath } from "node:url";
import { parse } from "node:querystring";
//#region src/loaders/config.ts
function createStandaloneConfigLoader({ core, buildConfig: buildConfig$1, mode }) {
	let prev;
	async function getConfigHash() {
		if (mode === "production") return "static";
		const stats = await fs.stat(core.configPath).catch(() => void 0);
		return stats ? stats.mtime.getTime() : "missing";
	}
	async function load(hash) {
		if (hash === "missing") return buildConfig({}, process.cwd());
		return (await loadConfig(core, buildConfig$1)).config;
	}
	return { async getCore() {
		const hash = await getConfigHash();
		if (!prev || hash !== prev.hash) prev = {
			hash,
			init: core.init({ config: load(hash) })
		};
		await prev.init;
		return core;
	} };
}
/**
* create config loader from initialized core
*/
function createIntegratedConfigLoader(core) {
	return { async getCore() {
		return core;
	} };
}
//#endregion
//#region src/loaders/adapter.ts
function toNode(test, loader) {
	return async (url, _context, nextLoad) => {
		if (url.startsWith("file:///") && (!test || test.test(url))) {
			const parsedUrl = new URL(url);
			const filePath = fileURLToPath(parsedUrl);
			const result = await loader.load({
				filePath,
				query: Object.fromEntries(parsedUrl.searchParams.entries()),
				async getSource() {
					return await fs.readFile(filePath, "utf-8");
				},
				development: false,
				compiler: { addDependency() {} }
			});
			if (result) return {
				source: result.code,
				format: "module",
				shortCircuit: true
			};
		}
		return nextLoad(url);
	};
}
function toVite(loader) {
	return { async transform(value, id) {
		const [file, query = ""] = id.split("?", 2);
		try {
			const parsedQuery = parse(query);
			if ("raw" in parsedQuery) return null;
			const result = await loader.load({
				filePath: file,
				query: parsedQuery,
				getSource() {
					return value;
				},
				development: this.environment ? this.environment.mode === "dev" : false,
				compiler: { addDependency: (file) => {
					this.addWatchFile(file);
				} }
			});
			if (result === null) return null;
			return {
				code: result.code,
				map: result.map,
				moduleType: result.moduleType
			};
		} catch (e) {
			if (e instanceof ValidationError) throw new Error(await e.toStringFormatted(), { cause: e });
			throw e;
		}
	} };
}
/**
* need to handle the `test` regex in Webpack config instead.
*/
function toWebpack(loader) {
	return async function(source, callback) {
		try {
			const result = await loader.load({
				filePath: this.resourcePath,
				query: parse(this.resourceQuery.slice(1)),
				getSource() {
					return source;
				},
				development: this.mode === "development",
				compiler: this
			});
			if (result === null) callback(void 0, source);
			else callback(void 0, result.code, result.map);
		} catch (error) {
			if (error instanceof ValidationError) return callback(new Error(await error.toStringFormatted()));
			if (!(error instanceof Error)) throw error;
			callback(error);
		}
	};
}
function toBun(test, loader) {
	function toResult(output) {
		if (!output) return;
		return {
			contents: output.code,
			loader: output.moduleType ?? "js"
		};
	}
	return (build) => {
		build.onLoad({ filter: test ?? /.+/ }, (args) => {
			const [filePath, query = ""] = args.path.split("?", 2);
			const input = {
				async getSource() {
					return Bun.file(filePath).text();
				},
				query: parse(query),
				filePath,
				development: false,
				compiler: { addDependency() {} }
			};
			if (loader.bun?.load) return loader.bun.load(readFileSync(filePath, "utf-8"), input);
			const result = loader.load(input);
			if (result instanceof Promise) return result.then(toResult);
			return toResult(result);
		});
	};
}
//#endregion
export { createIntegratedConfigLoader as a, toWebpack as i, toNode as n, createStandaloneConfigLoader as o, toVite as r, toBun as t };
