import { r as CoreOptions } from "../core-b6wXL1QY.js";
import { n as ResolvedMacroOptions, t as MacroPluginOption } from "../options-aB6xV-d7.js";
import { BunPlugin } from "bun";
//#region src/bun/index.d.ts
export interface MdxPluginOptions extends Pick<CoreOptions, 'configPath' | 'outDir' | 'plugins'> {
  /**
   * Configure the macro API (`fumadocs-mdx/macro`), or `false` to disable it.
   */
  macro?: MacroPluginOption;
  /**
   * Skip meta file transformation step
   */
  disableMetaFile?: boolean;
}
/**
 * @internal
 */
export declare function macroFilter(root: string, { include, exclude }: ResolvedMacroOptions): RegExp;
export declare function createMdxPlugin(options?: MdxPluginOptions): BunPlugin;
//#endregion