"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.defaultBM25Params = void 0;
exports.getPropertiesToSearch = getPropertiesToSearch;
exports.innerFullTextSearch = innerFullTextSearch;
exports.fullTextSearch = fullTextSearch;
exports.applyDefault = applyDefault;
const facets_js_1 = require("../components/facets.js");
const groups_js_1 = require("../components/groups.js");
const hooks_js_1 = require("../components/hooks.js");
const internal_document_id_store_js_1 = require("../components/internal-document-id-store.js");
const index_js_1 = require("../components/index.js");
const pinning_manager_js_1 = require("../components/pinning-manager.js");
const errors_js_1 = require("../errors.js");
const utils_js_1 = require("../utils.js");
const docs_js_1 = require("./docs.js");
const fetch_documents_js_1 = require("./fetch-documents.js");
function getPropertiesToSearch(zbsearch, properties) {
    const index = zbsearch.data.index;
    let propertiesToSearch = zbsearch.caches['propertiesToSearch'];
    if (!propertiesToSearch) {
        const propertiesToSearchWithTypes = zbsearch.index.getSearchablePropertiesWithTypes(index);
        propertiesToSearch = zbsearch.index.getSearchableProperties(index);
        propertiesToSearch = propertiesToSearch.filter((prop) => propertiesToSearchWithTypes[prop].startsWith('string'));
        zbsearch.caches['propertiesToSearch'] = propertiesToSearch;
    }
    if (properties && properties !== '*') {
        for (const prop of properties) {
            if (!propertiesToSearch.includes(prop)) {
                throw (0, errors_js_1.createError)('UNKNOWN_INDEX', prop, propertiesToSearch.join(', '));
            }
        }
        propertiesToSearch = propertiesToSearch.filter((prop) => properties.includes(prop));
    }
    return propertiesToSearch;
}
function innerFullTextSearch(zbsearch, params, language, precomputedWhereFiltersIDs) {
    const { term, properties } = params;
    const index = zbsearch.data.index;
    const propertiesToSearch = getPropertiesToSearch(zbsearch, properties);
    // If filters are enabled, we need to get the IDs of the documents that match the filters.
    const hasFilters = Object.keys(params.where ?? {}).length > 0;
    let whereFiltersIDs = precomputedWhereFiltersIDs;
    if (hasFilters && whereFiltersIDs === undefined) {
        whereFiltersIDs = zbsearch.index.searchByWhereClause(index, zbsearch.tokenizer, params.where, language);
    }
    let uniqueDocsIDs;
    // We need to perform the search if:
    // - we have a search term
    // - or we have properties to search
    //   in this case, we need to return all the documents that contains at least one of the given properties
    const threshold = params.threshold !== undefined && params.threshold !== null ? params.threshold : 1;
    if (term || properties) {
        const docsCount = (0, docs_js_1.count)(zbsearch);
        uniqueDocsIDs = zbsearch.index.search(index, term || '', zbsearch.tokenizer, language, propertiesToSearch, params.exact || false, params.tolerance || 0, params.boost || {}, applyDefault(params.relevance), docsCount, whereFiltersIDs, threshold, params.prefix);
        // When exact is true and we have a term, filter results to only include documents
        // where the original text contains the exact search term (case-sensitive).
        // This is a highly requested feature and although ZBSearch is not case-sensitive by design,
        // this is a reasonable compromise.
        if (params.exact && term) {
            const searchTerms = term.trim().split(/\s+/);
            const exactRegexes = searchTerms.map((searchTerm) => new RegExp(`\\b${escapeRegex(searchTerm)}\\b`));
            uniqueDocsIDs = uniqueDocsIDs.filter(([docId]) => {
                const doc = zbsearch.documentsStore.get(zbsearch.data.docs, docId);
                if (!doc)
                    return false;
                for (const prop of propertiesToSearch) {
                    const propValue = getPropValue(doc, prop);
                    if (typeof propValue === 'string') {
                        const hasAllTerms = exactRegexes.every((regex) => regex.test(propValue));
                        if (hasAllTerms) {
                            return true;
                        }
                    }
                }
                return false;
            });
        }
    }
    else {
        // Check if this is a geosearch-only query first
        if (hasFilters) {
            const geoResults = (0, index_js_1.searchByGeoWhereClause)(index, params.where);
            if (geoResults) {
                // This is a geosearch-only query with distance scoring
                uniqueDocsIDs = geoResults;
            }
            else {
                // Regular filter query without search term
                const docIds = whereFiltersIDs ? Array.from(whereFiltersIDs) : [];
                uniqueDocsIDs = docIds.map((k) => [+k, 0]);
            }
        }
        else {
            // No search term and no filters - return all documents
            const docIds = Object.keys(zbsearch.documentsStore.getAll(zbsearch.data.docs));
            uniqueDocsIDs = docIds.map((k) => [+k, 0]);
        }
    }
    return uniqueDocsIDs;
}
// Helper function to escape regex special characters
function escapeRegex(str) {
    return str.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}
// Helper function to get nested property value
function getPropValue(obj, path) {
    const keys = path.split('.');
    let value = obj;
    for (const key of keys) {
        if (value && typeof value === 'object' && key in value) {
            value = value[key];
        }
        else {
            return undefined;
        }
    }
    return value;
}
function fullTextSearch(zbsearch, params, language) {
    const timeStart = (0, utils_js_1.getNanosecondsTime)();
    function performSearchLogic() {
        const vectorProperties = Object.keys(zbsearch.data.index.vectorIndexes);
        const shouldCalculateFacets = params.facets && Object.keys(params.facets).length > 0;
        const { limit = 10, offset = 0, distinctOn, includeVectors = false } = params;
        const isPreflight = params.preflight === true;
        let uniqueDocsArray = innerFullTextSearch(zbsearch, params, language);
        if (params.sortBy) {
            if (typeof params.sortBy === 'function') {
                const ids = uniqueDocsArray.map(([id]) => id);
                const docs = zbsearch.documentsStore.getMultiple(zbsearch.data.docs, ids);
                const docsWithIdAndScore = docs.map((d, i) => [
                    uniqueDocsArray[i][0],
                    uniqueDocsArray[i][1],
                    d
                ]);
                docsWithIdAndScore.sort(params.sortBy);
                uniqueDocsArray = docsWithIdAndScore.map(([id, score]) => [id, score]);
            }
            else {
                uniqueDocsArray = zbsearch.sorter
                    .sortBy(zbsearch.data.sorting, uniqueDocsArray, params.sortBy)
                    .map(([id, score]) => [(0, internal_document_id_store_js_1.getInternalDocumentId)(zbsearch.internalDocumentIDStore, id), score]);
            }
        }
        else {
            uniqueDocsArray = uniqueDocsArray.sort(utils_js_1.sortTokenScorePredicate);
        }
        // Apply pinning rules after sorting but before pagination
        uniqueDocsArray = (0, pinning_manager_js_1.applyPinningRules)(zbsearch, zbsearch.data.pinning, uniqueDocsArray, params.term);
        let results;
        if (!isPreflight) {
            results = distinctOn
                ? (0, fetch_documents_js_1.fetchDocumentsWithDistinct)(zbsearch, uniqueDocsArray, offset, limit, distinctOn)
                : (0, fetch_documents_js_1.fetchDocuments)(zbsearch, uniqueDocsArray, offset, limit);
        }
        const searchResult = {
            elapsed: {
                formatted: '',
                raw: 0
            },
            hits: [],
            count: uniqueDocsArray.length
        };
        if (typeof results !== 'undefined') {
            searchResult.hits = results.filter(Boolean);
            if (!includeVectors) {
                (0, utils_js_1.removeVectorsFromHits)(searchResult, vectorProperties);
            }
        }
        if (shouldCalculateFacets) {
            const facets = (0, facets_js_1.getFacets)(zbsearch, uniqueDocsArray, params.facets);
            searchResult.facets = facets;
        }
        if (params.groupBy) {
            searchResult.groups = (0, groups_js_1.getGroups)(zbsearch, uniqueDocsArray, params.groupBy);
        }
        searchResult.elapsed = zbsearch.formatElapsedTime((0, utils_js_1.getNanosecondsTime)() - timeStart);
        return searchResult;
    }
    async function executeSearchAsync() {
        if (zbsearch.beforeSearch) {
            await (0, hooks_js_1.runBeforeSearch)(zbsearch.beforeSearch, zbsearch, params, language);
        }
        const searchResult = performSearchLogic();
        if (zbsearch.afterSearch) {
            await (0, hooks_js_1.runAfterSearch)(zbsearch.afterSearch, zbsearch, params, language, searchResult);
        }
        return searchResult;
    }
    const asyncNeeded = zbsearch.beforeSearch?.length || zbsearch.afterSearch?.length;
    if (asyncNeeded) {
        return executeSearchAsync();
    }
    return performSearchLogic();
}
exports.defaultBM25Params = {
    k: 1.2,
    b: 0.75,
    d: 0.5
};
function applyDefault(bm25Relevance) {
    const r = bm25Relevance ?? {};
    r.k = r.k ?? exports.defaultBM25Params.k;
    r.b = r.b ?? exports.defaultBM25Params.b;
    r.d = r.d ?? exports.defaultBM25Params.d;
    return r;
}
//# sourceMappingURL=search-fulltext.js.map