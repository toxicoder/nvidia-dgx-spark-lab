import { a as Separator$1, n as Item$1, o as definitions_d_exports, t as Folder$1 } from "../../definitions-BZlqdyOx.js";
import { C as LoaderPlugin } from "../../index-BEGAd2ej.js";
import { ReactNode } from "react";
//#region src/source/plugins/status-badges.d.ts
export interface Item extends Item$1 {
  /**
   * Status badge to display in the sidebar (e.g., "new", "beta", "deprecated", "experimental").
   */
  status?: string;
}
export interface Folder extends Omit<Folder$1, 'children' | 'index'> {
  index?: Item;
  children: Node[];
}
export type Separator = Separator$1;
export type Node = Item | Folder | Separator;
/**
 * Plugin to add status badges to pages in the sidebar.
 *
 * This reads the `status` field from page frontmatter and adds it to the page tree item.
 * Define the `renderBadge` option to render it.
 *
 * @example
 * ```tsx
 * import { loader } from 'fumadocs-core/source';
 * import { statusBadgesPlugin } from 'fumadocs-core/source/status-badges';
 *
 * export const source = loader({
 *   plugins: [
 *     statusBadgesPlugin({
 *       renderBadge: (status) => <span>{status}</span>,
 *     }),
 *   ],
 *   // ...
 * });
 * ```
 *
 * Then in your frontmatter:
 * ```yaml
 * ---
 * title: My Page
 * status: new
 * ---
 * ```
 */
export declare function statusBadgesPlugin(options?: {
  renderBadge?: (status: string) => ReactNode;
}): LoaderPlugin;
//#endregion