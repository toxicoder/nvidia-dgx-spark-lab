# Purpose: Example secrets for MCP dev stack
# Source of truth: n/a
# Regenerate: n/a
# Safety: copy and fill; never commit real secrets

# Example — create real secret from mcp/config/secrets.example.env (never commit values).
apiVersion: v1
kind: Secret
metadata:
  name: mcp-secrets
  namespace: agent-tools
type: Opaque
stringData:
  SEARXNG_SECRET: change-me
  CONTEXT7_API_KEY: ""
  GITEA_URL: https://gitea.example.com
  GITEA_TOKEN: ""
  FIRECRAWL_API_KEY: local-dev-key
  FIRECRAWL_BULL_AUTH_KEY: change-me
