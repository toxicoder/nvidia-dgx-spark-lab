# Purpose: Kustomize bundle for MCP qdrant
# Source of truth: mcp/k8s/workloads/qdrant/
# Regenerate: n/a
# Safety: agent-tools namespace; moderate resources

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - statefulset.yaml
  - service.yaml
