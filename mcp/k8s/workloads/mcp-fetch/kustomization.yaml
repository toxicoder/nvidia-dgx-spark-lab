# Purpose: Kustomize bundle for MCP mcp fetch
# Source of truth: mcp/k8s/workloads/mcp-fetch/
# Regenerate: n/a
# Safety: agent-tools namespace; moderate resources

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
