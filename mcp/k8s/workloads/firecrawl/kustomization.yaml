# Purpose: Kustomize bundle for MCP firecrawl
# Source of truth: mcp/k8s/workloads/firecrawl/
# Regenerate: n/a
# Safety: agent-tools namespace; moderate resources

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
