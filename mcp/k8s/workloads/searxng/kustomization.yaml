# Purpose: Kustomize bundle for MCP searxng
# Source of truth: mcp/k8s/workloads/searxng/
# Regenerate: n/a
# Safety: agent-tools namespace; moderate resources

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
