# Purpose: Kustomize bundle for MCP mem0
# Source of truth: mcp/k8s/workloads/mem0/
# Regenerate: n/a
# Safety: agent-tools namespace; moderate resources

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
